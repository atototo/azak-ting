# 뉴스 프로세스 개선 방향 분석 보고서

**작성일**: 2025-01-27  
**작성자**: Business Analyst (Mary)  
**분석 대상**: NEWS_COPYRIGHT_ANALYSIS.md + 현재 개발된 프로세스

---

## 1. 현재 프로세스 상세 분석

### 1.1 데이터 흐름도

```
[크롤러] → [NewsSaver] → [PostgreSQL] → [Milvus]
   ↓           ↓              ↓            ↓
전체 본문   전체 본문      전체 본문    벡터만 저장
스크래핑    저장          저장 (⚠️)    (✅ 안전)
```

### 1.2 뉴스 Content 사용 현황

**PostgreSQL 저장 (고위험 ⚠️)**
- 위치: `backend/db/models/news.py:52`
- 필드: `content = Column(Text, nullable=False)`
- 저장 내용: 전체 본문 텍스트
- 법적 리스크: **높음**

**임베딩 생성 (중위험)**
- 위치: `backend/llm/embedder.py:217`
- 코드: `texts = [f"{news.title}\n{news.content}" for news in unembedded_news]`
- 사용 목적: Milvus 벡터 검색용
- 법적 리스크: **중간** (벡터만 저장, 원본 텍스트 없음)

**유사 뉴스 검색 (중위험)**
- 위치: `backend/llm/vector_search.py`, `backend/crawlers/news_saver.py:223`
- 코드: `news_text = f"{news_article.title} {news_article.content}"`
- 사용 목적: 유사 뉴스 검색을 위한 임베딩 생성
- 법적 리스크: **중간** (임시 사용, 벡터만 저장)

**LLM 예측 프롬프트 (중위험)**
- 위치: `backend/llm/predictor.py:1188`
- 코드: `"내용": {current_news.get('content', 'N/A')[:300]}...`
- 사용 목적: GPT-4 예측 분석용
- 법적 리스크: **중간** (LLM에 전송, 저장 안 함)

**중복 검사 (중위험)**
- 위치: `backend/crawlers/news_saver.py:206`, `backend/notifications/auto_notify.py:74`
- 코드: `news_text = f"{news.title}\n{news.content}"`
- 사용 목적: 임베딩 기반 중복 뉴스 검사
- 법적 리스크: **중간** (임시 사용)

### 1.3 핵심 문제점

#### 문제 1: PostgreSQL에 전체 본문 저장
- **현재 상태**: 모든 크롤러가 전체 본문을 `content` 필드에 저장
- **법적 리스크**: ⚠️ **매우 높음**
- **영향 범위**: 
  - 저장: `backend/crawlers/news_saver.py:155`
  - 조회: 모든 예측/알림/검색 로직
  - 데이터베이스: 약 4,000건의 뉴스 본문 저장됨

#### 문제 2: 뉴스 중심 종목 추적 구조
- **현재 동작**: 뉴스가 발생한 종목만 추적
- **문제점**: 
  - 사용자가 관심 종목을 추가해도 뉴스 없으면 의미 없음
  - 종목 관리 페이지(49개)와 실제 분석 종목 불일치
- **영향**: 비즈니스 로직의 근본적 한계

#### 문제 3: HTML 스크래퍼 4개 (고위험)
- Naver 증권 (`naver_crawler.py`)
- 한국경제 (`hankyung_crawler.py`)
- 매일경제 (`maeil_crawler.py`)
- Naver 검색 (`naver_search_crawler.py`)
- **법적 리스크**: ⚠️ **매우 높음**

---

## 2. 개선 방향 제안

### 2.1 즉시 실행 가능한 조치 (단기: 1-2주)

#### 조치 1: Content 필드 제한 저장
**목표**: PostgreSQL에 전체 본문 대신 요약본만 저장

**변경 사항**:
```python
# 현재 (backend/crawlers/news_saver.py:155)
content = news_data.content  # 전체 본문

# 변경 후
content = self._truncate_content(news_data.content, max_length=200)
```

**구현 방법**:
1. `NewsSaver` 클래스에 `_truncate_content()` 메서드 추가
2. 본문을 200자로 제한 (제목 + 요약)
3. 기존 데이터는 마이그레이션 스크립트로 처리

**영향 분석**:
- ✅ 법적 리스크 대폭 감소
- ⚠️ 임베딩 품질 약간 저하 가능 (제목+요약으로도 충분할 가능성 높음)
- ✅ LLM 프롬프트 품질 유지 (이미 300자로 제한 중)

**우선순위**: 🔴 **최우선**

#### 조치 2: HTML 스크래퍼 비활성화
**목표**: 고위험 크롤러 4개 즉시 중단

**변경 사항**:
```python
# backend/scheduler/crawler_scheduler.py
# 다음 크롤러 비활성화:
# - NaverNewsCrawler
# - HankyungCrawler  
# - MaeilCrawler
# - NaverSearchCrawler (본문 스크래핑 부분만)
```

**대체 방안**:
- Naver 검색 API 메타데이터만 사용 (제목, 요약, URL)
- 본문 스크래핑 제거

**영향 분석**:
- ✅ 법적 리스크 즉시 제거
- ⚠️ 수집 뉴스 수 감소 (현재 3,400+건 → 예상 1,000건/일)
- ✅ 메타데이터만으로도 분석 가능 (제목+요약으로 임베딩 품질 유지 가능)

**우선순위**: 🔴 **최우선**

#### 조치 3: Reddit 크롤러 수정
**목표**: 비동기 환경 호환성 문제 해결

**현재 문제**:
- PRAW가 비동기 환경에서 작동 안 함
- 수집 데이터: 0건

**해결 방안**:
1. Async PRAW로 마이그레이션 (`asyncpraw` 라이브러리)
2. 또는 별도 동기 프로세스로 분리

**우선순위**: 🟡 **중간** (현재 데이터 0건이므로 급하지 않음)

---

### 2.2 중기 개선 방향 (1개월)

#### 개선 1: 종목 중심 추적 구조로 전환
**목표**: 뉴스 중심 → 종목 중심 아키텍처

**현재 구조**:
```
뉴스 발생 → 종목 추적 → 예측
```

**개선 구조**:
```
사용자 종목 선택 → 종목 추적 → 뉴스 수집 → 예측
```

**변경 사항**:
1. 종목 관리 페이지에서 사용자가 종목 추가
2. 해당 종목에 대한 뉴스만 수집 (Naver 검색 API)
3. 뉴스 없어도 종목 추적 유지

**구현 방법**:
- `backend/services/stock_tracking_service.py` 신규 생성
- 종목별 뉴스 수집 스케줄러 분리
- 뉴스 없이도 종목 분석 리포트 생성 가능하도록

**우선순위**: 🟡 **중간** (비즈니스 로직 개선)

#### 개선 2: 메타데이터 기반 분석 강화
**목표**: 본문 없이도 분석 품질 유지

**전략**:
1. 제목 + 요약(100자)으로 임베딩 생성
2. LLM 프롬프트에 메타데이터 강화 (발행일, 출처, 키워드)
3. 유사 뉴스 검색 품질 검증

**검증 방법**:
- A/B 테스트: 전체 본문 vs 메타데이터만
- 예측 정확도 비교

**우선순위**: 🟢 **낮음** (단기 조치 후 검증)

---

### 2.3 장기 전략 (3개월)

#### 전략 1: 합법적 뉴스 소스 확보
**옵션 A: Naver 검색 API 파트너십**
- Naver 검색 API 계약
- 본문 제공 라이선스 협상
- 비용: 예상 월 100만원 이상

**옵션 B: 언론사 직접 계약**
- 한국경제, 매일경제 API 계약
- 비용: 예상 월 200-500만원

**옵션 C: 뉴스 애그리게이터 서비스**
- NewsAPI, Google News API
- 비용: 예상 월 50-200만원

**우선순위**: 🟢 **낮음** (비즈니스 성장 후)

#### 전략 2: 대체 데이터 소스 강화
**목표**: 뉴스 의존도 감소

**대체 소스**:
1. **DART 공시** (이미 사용 중 ✅)
2. **KIS API** (한국투자증권) - 가격/거래량 데이터
3. **재무제표 분석** (DART에서 확장)
4. **소셜 미디어** (Reddit, Twitter) - 감성 분석
5. **기술적 지표** (RSI, MACD 등) - 이미 구현됨 ✅

**우선순위**: 🟡 **중간** (차별화 요소)

---

## 3. 구체적 구현 계획

### Phase 1: 법적 리스크 최소화 (1-2주)

#### 작업 1.1: Content 필드 제한 저장
**파일**: `backend/crawlers/news_saver.py`
**변경 내용**:
```python
def _truncate_content(self, content: str, max_length: int = 200) -> str:
    """본문을 제한된 길이로 자릅니다."""
    if len(content) <= max_length:
        return content
    return content[:max_length] + "..."
```

**마이그레이션 스크립트**:
```python
# scripts/migrate_content_truncate.py
# 기존 content를 200자로 제한
```

#### 작업 1.2: HTML 스크래퍼 비활성화
**파일**: `backend/scheduler/crawler_scheduler.py`
**변경 내용**:
- 4개 크롤러 스케줄 제거 또는 주석 처리
- Naver 검색 API만 사용 (메타데이터)

#### 작업 1.3: Naver 검색 API 본문 스크래핑 제거
**파일**: `backend/crawlers/naver_search_crawler.py`
**변경 내용**:
- 본문 스크래핑 로직 제거
- 메타데이터(제목, 요약, URL)만 사용

### Phase 2: 시스템 안정화 (2-4주)

#### 작업 2.1: 메타데이터 기반 분석 품질 검증
- A/B 테스트 설계
- 예측 정확도 비교
- 임베딩 품질 측정

#### 작업 2.2: Reddit 크롤러 수정
- Async PRAW 마이그레이션
- 테스트 및 검증

### Phase 3: 아키텍처 개선 (1-2개월)

#### 작업 3.1: 종목 중심 추적 구조
- 종목 추적 서비스 신규 개발
- 뉴스 수집 로직 재설계

---

## 4. 리스크 평가

### 4.1 법적 리스크

| 조치 | 현재 리스크 | 개선 후 리스크 | 개선 효과 |
|------|------------|---------------|----------|
| Content 필드 제한 | ⚠️ 높음 | 🟢 낮음 | ✅ 대폭 감소 |
| HTML 스크래퍼 중단 | ⚠️ 매우 높음 | ✅ 없음 | ✅ 완전 제거 |
| 메타데이터만 사용 | ⚠️ 중간 | 🟢 낮음 | ✅ 감소 |

### 4.2 비즈니스 리스크

| 조치 | 현재 상태 | 개선 후 | 리스크 |
|------|----------|---------|--------|
| 뉴스 수집량 | 4,000건 | 1,000건/일 | 🟡 중간 (품질 유지 가능) |
| 분석 품질 | 높음 | 중간-높음 | 🟢 낮음 (검증 필요) |
| 사용자 경험 | 양호 | 유지 | 🟢 낮음 |

---

## 5. 의사결정 체크리스트

### 즉시 실행 결정 필요

- [ ] **Content 필드 제한 저장** - 승인 여부
- [ ] **HTML 스크래퍼 비활성화** - 승인 여부
- [ ] **기존 데이터 처리 방안** - 마이그레이션 vs 유지

### 중기 검토 필요

- [ ] **종목 중심 추적 구조 전환** - 비즈니스 우선순위
- [ ] **메타데이터 분석 품질 검증** - A/B 테스트 설계
- [ ] **Reddit 크롤러 수정** - 우선순위 결정

### 장기 전략 수립 필요

- [ ] **합법적 뉴스 소스 확보** - 예산 및 계약 계획
- [ ] **대체 데이터 소스 강화** - 로드맵 수립

---

## 6. 추천 실행 순서

### 최우선 (이번 주)
1. ✅ HTML 스크래퍼 4개 비활성화
2. ✅ Content 필드 200자 제한 저장
3. ✅ Naver 검색 API 본문 스크래핑 제거

### 단기 (2주 내)
4. ✅ 기존 데이터 마이그레이션 (선택사항)
5. ✅ 메타데이터 기반 분석 품질 검증

### 중기 (1개월 내)
6. ✅ Reddit 크롤러 수정
7. ✅ 종목 중심 추적 구조 설계

### 장기 (3개월 내)
8. ✅ 합법적 뉴스 소스 확보 검토
9. ✅ 대체 데이터 소스 강화

---

## 7. 결론

현재 시스템은 **법적 리스크가 높은 상태**입니다. 특히 PostgreSQL에 전체 본문을 저장하는 것은 즉시 개선이 필요합니다.

**즉시 실행 가능한 조치**로 법적 리스크를 최소화하면서, **비즈니스 가치를 유지**할 수 있습니다:

1. ✅ 메타데이터(제목+요약)만으로도 분석 품질 유지 가능
2. ✅ 벡터 임베딩은 원본 텍스트 저장 없이 안전
3. ✅ LLM 프롬프트는 이미 300자로 제한되어 있음

**권장 사항**: Phase 1 조치를 즉시 실행하여 법적 리스크를 제거하고, 이후 단계적으로 시스템을 개선하는 것이 최선입니다.

---

**다음 단계**: 
1. 이 보고서 검토 및 승인
2. Phase 1 구현 시작
3. 품질 검증 및 모니터링

